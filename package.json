{
  "name": "browserify-test",
  "version": "1.0.0",
  "scripts": {
    "pack": "browserify main.ts -p [ tsify --noImplicitAny ] > bundle.js"
  },
  "dependencies": {
    "uniq": "^1.0.1"
  },
  "devDependencies": {
    "browserify": "^13.1.0",
    "tsify": "^1.0.3",
    "typescript": "^1.8.10"
  }
}
